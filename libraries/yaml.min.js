// YAML-MIN from https://github.com/wellwelwel/yaml.min

var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};

// src/internal/chars.ts
var CHAR_BOM = 65279;
var CHAR_TAB = 9;
var CHAR_LF = 10;
var CHAR_CR = 13;
var CHAR_SPACE = 32;
var CHAR_EXCL = 33;
var CHAR_DQUOTE = 34;
var CHAR_HASH = 35;
var CHAR_PERCENT = 37;
var CHAR_AMP = 38;
var CHAR_SQUOTE = 39;
var CHAR_STAR = 42;
var CHAR_PLUS = 43;
var CHAR_COMMA = 44;
var CHAR_MINUS = 45;
var CHAR_DOT = 46;
var CHAR_0 = 48;
var CHAR_1 = 49;
var CHAR_9 = 57;
var CHAR_COLON = 58;
var CHAR_GT = 62;
var CHAR_QUESTION = 63;
var CHAR_UPPER_A = 65;
var CHAR_UPPER_F = 70;
var CHAR_UPPER_N = 78;
var CHAR_UPPER_T = 84;
var CHAR_UPPER_U = 85;
var CHAR_LBRACKET = 91;
var CHAR_BACKSLASH = 92;
var CHAR_RBRACKET = 93;
var CHAR_UNDERSCORE = 95;
var CHAR_LOWER_F = 102;
var CHAR_LOWER_N = 110;
var CHAR_LOWER_O = 111;
var CHAR_LOWER_T = 116;
var CHAR_LOWER_U = 117;
var CHAR_LOWER_X = 120;
var CHAR_LBRACE = 123;
var CHAR_PIPE = 124;
var CHAR_RBRACE = 125;
var CHAR_TILDE = 126;
var NON_PRINTABLE = /[\x00-\x08\x0B\x0C\x0E-\x1F\x7F-\x84\x86-\x9F\uFFFE\uFFFF]|[\uD800-\uDBFF](?![\uDC00-\uDFFF])|(?:[^\uD800-\uDBFF]|^)[\uDC00-\uDFFF]/;
var SIMPLE_ESCAPE_MAP = (() => {
  const map = new Array(128);
  const escapes = {
    48: "\0",
    97: "\x07",
    98: "\b",
    101: "\x1B",
    102: "\f",
    110: "\n",
    114: "\r",
    116: "	",
    118: "\v",
    78: "\x85",
    95: "\xA0",
    76: "\u2028",
    80: "\u2029",
    32: " ",
    34: '"',
    47: "/",
    92: "\\"
  };
  for (const code of Object.keys(escapes))
    map[Number(code)] = escapes[Number(code)];
  return map;
})();
var RE_FLOAT = /^[-+]?(\.[0-9]+|[0-9]+(\.[0-9]*)?)([eE][-+]?[0-9]+)?$/;
var RE_INFINITY = /^[+-]?\.(inf|Inf|INF)$/;
var isDecimal = (code) => code >= CHAR_0 && code <= CHAR_9;
var isHexChar = (code) => code >= CHAR_0 && code <= CHAR_9 || code >= CHAR_UPPER_A && code <= CHAR_UPPER_F || code >= 97 && code <= CHAR_LOWER_F;
var isOctalChar = (code) => code >= CHAR_0 && code <= 55;

// src/internal/navigation.ts
var skipSpaces = (state) => {
  while (state.pos < state.len) {
    const code = state.src.charCodeAt(state.pos);
    if (code !== CHAR_SPACE && code !== CHAR_TAB) break;
    state.pos++;
  }
};
var skipComment = (state) => {
  if (state.pos < state.len && state.src.charCodeAt(state.pos) === CHAR_HASH) {
    while (state.pos < state.len && state.src.charCodeAt(state.pos) !== CHAR_LF)
      state.pos++;
  }
};
var skipBlanksAndComments = (state) => {
  while (state.pos < state.len) {
    const code = state.src.charCodeAt(state.pos);
    if (code === CHAR_SPACE || code === CHAR_TAB || code === CHAR_CR || code === CHAR_LF) {
      state.pos++;
    } else if (code === CHAR_HASH) {
      while (state.pos < state.len && state.src.charCodeAt(state.pos) !== CHAR_LF)
        state.pos++;
    } else {
      break;
    }
  }
};
var skipNewlinesAndComments = (state) => {
  while (state.pos < state.len) {
    const code = state.src.charCodeAt(state.pos);
    if (code === CHAR_LF) {
      state.pos++;
    } else if (code === CHAR_CR) {
      state.pos++;
      if (state.pos < state.len && state.src.charCodeAt(state.pos) === CHAR_LF)
        state.pos++;
    } else {
      const lineStart = state.pos;
      while (state.pos < state.len && state.src.charCodeAt(state.pos) === CHAR_SPACE)
        state.pos++;
      if (state.pos < state.len && state.src.charCodeAt(state.pos) === CHAR_HASH) {
        while (state.pos < state.len && state.src.charCodeAt(state.pos) !== CHAR_LF)
          state.pos++;
      } else {
        return state.pos - lineStart;
      }
    }
  }
  return 0;
};
var peekLineIndent = (state) => {
  let cursor = state.pos;
  while (cursor < state.len) {
    if (state.src.charCodeAt(cursor) !== CHAR_SPACE) break;
    cursor++;
  }
  return cursor - state.pos;
};
var skipDirectivesAndMarkers = (state) => {
  while (state.pos < state.len) {
    skipBlanksAndComments(state);
    if (state.pos >= state.len) break;
    if (state.src.charCodeAt(state.pos) === CHAR_PERCENT) {
      while (state.pos < state.len && state.src.charCodeAt(state.pos) !== CHAR_LF)
        state.pos++;
      continue;
    }
    if (state.src.charCodeAt(state.pos) === CHAR_MINUS && state.src.charCodeAt(state.pos + 1) === CHAR_MINUS && state.src.charCodeAt(state.pos + 2) === CHAR_MINUS) {
      state.pos += 3;
      if (state.pos < state.len) {
        const afterMarker = state.src.charCodeAt(state.pos);
        if (afterMarker !== CHAR_SPACE && afterMarker !== CHAR_LF && afterMarker !== CHAR_CR && afterMarker !== CHAR_TAB) {
          state.pos -= 3;
          break;
        }
      }
      skipSpaces(state);
      skipComment(state);
      if (state.pos < state.len) {
        const newlineCode = state.src.charCodeAt(state.pos);
        if (newlineCode === CHAR_CR) {
          state.pos++;
          if (state.pos < state.len && state.src.charCodeAt(state.pos) === CHAR_LF)
            state.pos++;
        } else if (newlineCode === CHAR_LF) {
          state.pos++;
        }
      }
      continue;
    }
    if (state.src.charCodeAt(state.pos) === CHAR_DOT && state.src.charCodeAt(state.pos + 1) === CHAR_DOT && state.src.charCodeAt(state.pos + 2) === CHAR_DOT) {
      state.pos += 3;
      while (state.pos < state.len && state.src.charCodeAt(state.pos) !== CHAR_LF)
        state.pos++;
      if (state.pos < state.len) state.pos++;
      continue;
    }
    break;
  }
};
var readAnchorOrTag = (state) => {
  state.lastAnchor = void 0;
  while (state.pos < state.len) {
    skipSpaces(state);
    const code = state.src.charCodeAt(state.pos);
    if (code !== CHAR_AMP && code !== CHAR_EXCL) break;
    if (code === CHAR_AMP) {
      state.pos++;
      const start = state.pos;
      while (state.pos < state.len) {
        const charCode = state.src.charCodeAt(state.pos);
        if (charCode === CHAR_SPACE || charCode === CHAR_TAB || charCode === CHAR_LF || charCode === CHAR_CR || charCode === CHAR_COMMA || charCode === CHAR_RBRACKET || charCode === CHAR_RBRACE || charCode === CHAR_COLON)
          break;
        state.pos++;
      }
      state.lastAnchor = state.src.slice(start, state.pos);
      continue;
    }
    while (state.pos < state.len) {
      const charCode = state.src.charCodeAt(state.pos);
      if (charCode === CHAR_SPACE || charCode === CHAR_TAB || charCode === CHAR_LF || charCode === CHAR_CR)
        break;
      state.pos++;
    }
  }
};
var parseAlias = (state) => {
  state.pos++;
  const start = state.pos;
  while (state.pos < state.len) {
    const charCode = state.src.charCodeAt(state.pos);
    if (charCode === CHAR_SPACE || charCode === CHAR_TAB || charCode === CHAR_LF || charCode === CHAR_CR || charCode === CHAR_COMMA || charCode === CHAR_RBRACKET || charCode === CHAR_RBRACE || charCode === CHAR_COLON)
      break;
    state.pos++;
  }
  const name = state.src.slice(start, state.pos);
  if (state.anchors.has(name)) return state.anchors.get(name);
  return void 0;
};
var withAnchor = (state, anchor, value) => {
  if (anchor) state.anchors.set(anchor, value);
  return value;
};

// src/internal/scalars.ts
var resolveInteger = (raw) => {
  const length = raw.length;
  let index = 0;
  let code = raw.charCodeAt(index);
  if (code === CHAR_MINUS || code === CHAR_PLUS) {
    code = raw.charCodeAt(++index);
  }
  if (code === CHAR_0 && index + 1 < length) {
    const prefix = raw.charCodeAt(index + 1);
    if (prefix === CHAR_LOWER_O) {
      for (let charIndex = index + 2; charIndex < length; charIndex++) {
        if (!isOctalChar(raw.charCodeAt(charIndex))) return void 0;
      }
      return index + 2 < length ? Number.parseInt(raw.slice(index + 2), 8) : void 0;
    }
    if (prefix === CHAR_LOWER_X) {
      for (let charIndex = index + 2; charIndex < length; charIndex++) {
        if (!isHexChar(raw.charCodeAt(charIndex))) return void 0;
      }
      return index + 2 < length ? Number.parseInt(raw.slice(index + 2), 16) : void 0;
    }
  }
  if (index >= length) return void 0;
  for (let charIndex = index; charIndex < length; charIndex++) {
    if (!isDecimal(raw.charCodeAt(charIndex))) return void 0;
  }
  return Number.parseInt(raw, 10);
};
var resolveScalar = (raw) => {
  if (raw === "") return null;
  const firstCharCode = raw.charCodeAt(0);
  if (firstCharCode === CHAR_TILDE && raw.length === 1) return null;
  if ((firstCharCode === CHAR_LOWER_N || firstCharCode === CHAR_UPPER_N) && (raw === "null" || raw === "Null" || raw === "NULL"))
    return null;
  if ((firstCharCode === CHAR_LOWER_T || firstCharCode === CHAR_UPPER_T) && (raw === "true" || raw === "True" || raw === "TRUE"))
    return true;
  if ((firstCharCode === CHAR_LOWER_F || firstCharCode === CHAR_UPPER_F) && (raw === "false" || raw === "False" || raw === "FALSE"))
    return false;
  if (firstCharCode >= CHAR_0 && firstCharCode <= CHAR_9 || firstCharCode === CHAR_PLUS || firstCharCode === CHAR_MINUS || firstCharCode === CHAR_DOT) {
    const integer = resolveInteger(raw);
    if (integer !== void 0) return integer;
    if (RE_FLOAT.test(raw) && raw !== "+" && raw !== "-")
      return Number.parseFloat(raw);
    if (RE_INFINITY.test(raw))
      return raw[0] === "-" ? Number.NEGATIVE_INFINITY : Number.POSITIVE_INFINITY;
    if (raw === ".nan" || raw === ".NaN" || raw === ".NAN") return Number.NaN;
  }
  return raw;
};
var trimEnd = (str) => {
  let cursor = str.length;
  while (cursor > 0) {
    const code = str.charCodeAt(cursor - 1);
    if (code !== CHAR_SPACE && code !== CHAR_TAB && code !== CHAR_LF && code !== CHAR_CR)
      break;
    cursor--;
  }
  return cursor < str.length ? str.slice(0, cursor) : str;
};
var setMapKey = (target, key, value) => {
  if (key.charCodeAt(0) === CHAR_UNDERSCORE && key === "__proto__") {
    Object.defineProperty(target, key, {
      value,
      writable: true,
      enumerable: true,
      configurable: true
    });
  } else {
    target[key] = value;
  }
};
var stringifyKey = (key) => {
  if (typeof key === "object" && key !== null) return JSON.stringify(key);
  return String(key == null ? "" : key);
};

// src/internal/strings.ts
var parseSingleQuoted = (state) => {
  state.pos++;
  let result = "";
  let captureStart = state.pos;
  while (state.pos < state.len) {
    const code = state.src.charCodeAt(state.pos);
    if (code !== CHAR_SQUOTE && code !== CHAR_LF && code !== CHAR_CR) {
      state.pos++;
      continue;
    }
    if (code === CHAR_SQUOTE) {
      result += state.src.slice(captureStart, state.pos);
      if (state.src.charCodeAt(state.pos + 1) === CHAR_SQUOTE) {
        result += "'";
        state.pos += 2;
        captureStart = state.pos;
        continue;
      }
      state.pos++;
      return result;
    }
    result += state.src.slice(captureStart, state.pos);
    if (code === CHAR_CR) state.pos++;
    if (state.pos < state.len && state.src.charCodeAt(state.pos) === CHAR_LF)
      state.pos++;
    let emptyLineCount = 0;
    while (state.pos < state.len) {
      const whitespaceCode = state.src.charCodeAt(state.pos);
      if (whitespaceCode === CHAR_LF) emptyLineCount++;
      else if (whitespaceCode === CHAR_CR) {
        emptyLineCount++;
        if (state.src.charCodeAt(state.pos + 1) === CHAR_LF) state.pos++;
      } else if (whitespaceCode !== CHAR_SPACE && whitespaceCode !== CHAR_TAB)
        break;
      state.pos++;
    }
    result += emptyLineCount > 0 ? "\n".repeat(emptyLineCount) : " ";
    captureStart = state.pos;
  }
  return result + state.src.slice(captureStart, state.pos);
};
var parseDoubleQuoted = (state) => {
  state.pos++;
  let result = "";
  let captureStart = state.pos;
  while (state.pos < state.len) {
    const code = state.src.charCodeAt(state.pos);
    if (code !== CHAR_DQUOTE && code !== CHAR_BACKSLASH && code !== CHAR_LF && code !== CHAR_CR) {
      state.pos++;
      continue;
    }
    if (code === CHAR_DQUOTE) {
      result += state.src.slice(captureStart, state.pos);
      state.pos++;
      return result;
    }
    if (code === CHAR_BACKSLASH) {
      result += state.src.slice(captureStart, state.pos);
      state.pos++;
      const escapeCode = state.src.charCodeAt(state.pos);
      if (escapeCode === CHAR_LF || escapeCode === CHAR_CR) {
        if (escapeCode === CHAR_CR) state.pos++;
        if (state.pos < state.len && state.src.charCodeAt(state.pos) === CHAR_LF)
          state.pos++;
        while (state.pos < state.len && (state.src.charCodeAt(state.pos) === CHAR_SPACE || state.src.charCodeAt(state.pos) === CHAR_TAB))
          state.pos++;
        captureStart = state.pos;
        continue;
      }
      const mapped = escapeCode < 128 ? SIMPLE_ESCAPE_MAP[escapeCode] : void 0;
      if (mapped !== void 0) {
        result += mapped;
        state.pos++;
      } else if (escapeCode === CHAR_LOWER_X) {
        state.pos++;
        result += String.fromCharCode(
          Number.parseInt(state.src.slice(state.pos, state.pos + 2), 16)
        );
        state.pos += 2;
      } else if (escapeCode === CHAR_LOWER_U) {
        state.pos++;
        result += String.fromCharCode(
          Number.parseInt(state.src.slice(state.pos, state.pos + 4), 16)
        );
        state.pos += 4;
      } else if (escapeCode === CHAR_UPPER_U) {
        state.pos++;
        result += String.fromCodePoint(
          Number.parseInt(state.src.slice(state.pos, state.pos + 8), 16)
        );
        state.pos += 8;
      } else {
        result += state.src[state.pos];
        state.pos++;
      }
      captureStart = state.pos;
      continue;
    }
    result += state.src.slice(captureStart, state.pos);
    if (code === CHAR_CR) state.pos++;
    if (state.pos < state.len && state.src.charCodeAt(state.pos) === CHAR_LF)
      state.pos++;
    let emptyLineCount = 0;
    while (state.pos < state.len) {
      const whitespaceCode = state.src.charCodeAt(state.pos);
      if (whitespaceCode === CHAR_LF) emptyLineCount++;
      else if (whitespaceCode === CHAR_CR) {
        emptyLineCount++;
        if (state.src.charCodeAt(state.pos + 1) === CHAR_LF) state.pos++;
      } else if (whitespaceCode !== CHAR_SPACE && whitespaceCode !== CHAR_TAB)
        break;
      state.pos++;
    }
    result += emptyLineCount > 0 ? "\n".repeat(emptyLineCount) : " ";
    captureStart = state.pos;
  }
  return result + state.src.slice(captureStart, state.pos);
};
var foldBlockLines = (contentLines) => {
  let result = "";
  let prevMoreIndented = false;
  for (let lineIndex = 0; lineIndex < contentLines.length; lineIndex++) {
    const line = contentLines[lineIndex];
    if (line === "") {
      result += "\n";
      continue;
    }
    if (line[0] === " " || line[0] === "	") {
      if (result.length > 0 && !result.endsWith("\n")) result += "\n";
      result += line;
      if (lineIndex < contentLines.length - 1) result += "\n";
      prevMoreIndented = true;
      continue;
    }
    if (lineIndex > 0 && !prevMoreIndented && contentLines[lineIndex - 1] !== "") {
      result += " ";
    } else if (prevMoreIndented) {
      result += "\n";
    }
    result += line;
    prevMoreIndented = false;
  }
  return result;
};
var parseBlockScalar = (state, baseIndent) => {
  const isLiteral = state.src.charCodeAt(state.pos) === CHAR_PIPE;
  state.pos++;
  let chomp = "";
  let explicitIndent = 0;
  while (state.pos < state.len) {
    const headerCode = state.src.charCodeAt(state.pos);
    if (headerCode === CHAR_LF || headerCode === CHAR_CR || headerCode === CHAR_HASH)
      break;
    if (headerCode === CHAR_MINUS) chomp = "-";
    else if (headerCode === CHAR_PLUS) chomp = "+";
    else if (headerCode >= CHAR_1 && headerCode <= CHAR_9)
      explicitIndent = headerCode - CHAR_0;
    else if (headerCode !== CHAR_SPACE && headerCode !== CHAR_TAB) break;
    state.pos++;
  }
  skipSpaces(state);
  skipComment(state);
  if (state.pos < state.len) {
    const newlineAfterHeader = state.src.charCodeAt(state.pos);
    if (newlineAfterHeader === CHAR_CR || newlineAfterHeader === CHAR_LF) {
      if (newlineAfterHeader === CHAR_CR && state.src.charCodeAt(state.pos + 1) === CHAR_LF)
        state.pos++;
      state.pos++;
    }
  }
  let contentIndent = 0;
  if (explicitIndent > 0) {
    contentIndent = baseIndent + explicitIndent;
  } else {
    const savedPos = state.pos;
    while (state.pos < state.len) {
      let spaces = 0;
      while (state.pos < state.len && state.src.charCodeAt(state.pos) === CHAR_SPACE) {
        spaces++;
        state.pos++;
      }
      if (state.pos < state.len) {
        const indentLineCode = state.src.charCodeAt(state.pos);
        if (indentLineCode !== CHAR_LF && indentLineCode !== CHAR_CR) {
          contentIndent = spaces;
          break;
        }
        if (indentLineCode === CHAR_CR) state.pos++;
        if (state.pos < state.len && state.src.charCodeAt(state.pos) === CHAR_LF)
          state.pos++;
      }
    }
    state.pos = savedPos;
  }
  if (contentIndent === 0) {
    if (chomp === "+") return "\n";
    return "";
  }
  const lines = [];
  while (state.pos < state.len) {
    const lineStart = state.pos;
    let spaces = 0;
    while (state.pos < state.len && state.src.charCodeAt(state.pos) === CHAR_SPACE) {
      spaces++;
      state.pos++;
    }
    if (state.pos >= state.len) {
      lines.push("");
      break;
    }
    const lineEndCode = state.src.charCodeAt(state.pos);
    if (lineEndCode === CHAR_LF || lineEndCode === CHAR_CR) {
      lines.push("");
      if (lineEndCode === CHAR_CR && state.src.charCodeAt(state.pos + 1) === CHAR_LF)
        state.pos++;
      state.pos++;
      continue;
    }
    if (spaces < contentIndent) {
      state.pos = lineStart;
      break;
    }
    const prefix = spaces > contentIndent ? " ".repeat(spaces - contentIndent) : "";
    const lineContentStart = state.pos;
    while (state.pos < state.len) {
      const contentCode = state.src.charCodeAt(state.pos);
      if (contentCode === CHAR_LF || contentCode === CHAR_CR) break;
      state.pos++;
    }
    lines.push(prefix + state.src.slice(lineContentStart, state.pos));
    if (state.pos < state.len) {
      if (state.src.charCodeAt(state.pos) === CHAR_CR && state.src.charCodeAt(state.pos + 1) === CHAR_LF)
        state.pos++;
      state.pos++;
    }
  }
  let trailingEmpties = 0;
  for (let lineIndex = lines.length - 1; lineIndex >= 0; lineIndex--) {
    if (lines[lineIndex] === "") trailingEmpties++;
    else break;
  }
  const contentLines = trailingEmpties > 0 ? lines.slice(0, lines.length - trailingEmpties) : lines;
  const content = isLiteral ? contentLines.join("\n") : foldBlockLines(contentLines);
  if (chomp === "-") return content;
  if (chomp === "+") return `${content}
${"\n".repeat(trailingEmpties)}`;
  return `${content}
`;
};
var capturePlainLine = (state, inFlow) => {
  const captureStart = state.pos;
  while (state.pos < state.len) {
    const charCode = state.src.charCodeAt(state.pos);
    if (charCode === CHAR_LF || charCode === CHAR_CR) break;
    if (charCode === CHAR_HASH && state.pos > 0) {
      const prevCode = state.src.charCodeAt(state.pos - 1);
      if (prevCode === CHAR_SPACE || prevCode === CHAR_TAB) break;
    }
    if (charCode === CHAR_COLON) {
      const afterColon = state.pos + 1 >= state.len ? -1 : state.src.charCodeAt(state.pos + 1);
      if (!inFlow && (afterColon === -1 || afterColon === CHAR_SPACE || afterColon === CHAR_LF || afterColon === CHAR_CR || afterColon === CHAR_TAB))
        break;
      if (inFlow && (afterColon === -1 || afterColon === CHAR_SPACE || afterColon === CHAR_COMMA || afterColon === CHAR_RBRACKET || afterColon === CHAR_RBRACE))
        break;
    }
    if (inFlow && (charCode === CHAR_COMMA || charCode === CHAR_RBRACKET || charCode === CHAR_RBRACE || charCode === CHAR_LBRACE || charCode === CHAR_LBRACKET))
      break;
    state.pos++;
  }
  return state.src.slice(captureStart, state.pos);
};
var parsePlainScalar = (state, minIndent, inFlow) => {
  let result = capturePlainLine(state, inFlow);
  while (state.pos < state.len) {
    const savedPos = state.pos;
    let emptyLineCount = 0;
    while (state.pos < state.len) {
      const whitespaceCode = state.src.charCodeAt(state.pos);
      if (whitespaceCode === CHAR_LF) emptyLineCount++;
      else if (whitespaceCode === CHAR_CR) {
        emptyLineCount++;
        if (state.src.charCodeAt(state.pos + 1) === CHAR_LF) state.pos++;
      } else if (whitespaceCode !== CHAR_SPACE && whitespaceCode !== CHAR_TAB)
        break;
      state.pos++;
    }
    const lineIndent = peekLineIndent(state);
    if (state.pos >= state.len || lineIndent <= minIndent) {
      state.pos = savedPos;
      break;
    }
    const peekedCode = state.src.charCodeAt(state.pos);
    if (peekedCode === CHAR_HASH) {
      state.pos = savedPos;
      break;
    }
    if (!inFlow && (peekedCode === CHAR_MINUS || peekedCode === CHAR_COLON || peekedCode === CHAR_QUESTION)) {
      const indicatorNext = state.pos + 1 >= state.len ? -1 : state.src.charCodeAt(state.pos + 1);
      if (indicatorNext === -1 || indicatorNext === CHAR_SPACE || indicatorNext === CHAR_LF || indicatorNext === CHAR_CR || indicatorNext === CHAR_TAB) {
        state.pos = savedPos;
        break;
      }
    }
    result += emptyLineCount > 1 ? "\n".repeat(emptyLineCount - 1) : " ";
    result += capturePlainLine(state, inFlow);
  }
  return trimEnd(result);
};

// src/internal/flow.ts
var parseFlowSeq = (state) => {
  state.pos++;
  const result = [];
  skipBlanksAndComments(state);
  while (state.pos < state.len && state.src.charCodeAt(state.pos) !== CHAR_RBRACKET) {
    readAnchorOrTag(state);
    const anchor = state.lastAnchor;
    skipSpaces(state);
    const value = parseFlowValue(state);
    if (anchor) state.anchors.set(anchor, value);
    result.push(value);
    skipBlanksAndComments(state);
    if (state.pos < state.len && state.src.charCodeAt(state.pos) === CHAR_COMMA) {
      state.pos++;
      skipBlanksAndComments(state);
    }
  }
  if (state.pos < state.len) state.pos++;
  return result;
};
var parseFlowMap = (state) => {
  state.pos++;
  const result = {};
  skipBlanksAndComments(state);
  while (state.pos < state.len && state.src.charCodeAt(state.pos) !== CHAR_RBRACE) {
    if (state.src.charCodeAt(state.pos) === CHAR_QUESTION) {
      const questionNext = state.src.charCodeAt(state.pos + 1);
      if (questionNext === CHAR_SPACE || questionNext === CHAR_TAB || questionNext === CHAR_LF || questionNext === CHAR_CR) {
        state.pos++;
        skipBlanksAndComments(state);
      }
    }
    readAnchorOrTag(state);
    const keyAnchor = state.lastAnchor;
    skipSpaces(state);
    const key = parseFlowScalarKey(state);
    if (keyAnchor) state.anchors.set(keyAnchor, key);
    skipBlanksAndComments(state);
    let value = null;
    if (state.pos < state.len && state.src.charCodeAt(state.pos) === CHAR_COLON) {
      state.pos++;
      skipBlanksAndComments(state);
      if (state.pos < state.len) {
        const valueCode = state.src.charCodeAt(state.pos);
        if (valueCode !== CHAR_COMMA && valueCode !== CHAR_RBRACE) {
          readAnchorOrTag(state);
          const valAnchor = state.lastAnchor;
          skipSpaces(state);
          value = parseFlowValue(state);
          if (valAnchor) state.anchors.set(valAnchor, value);
        }
      }
    }
    setMapKey(result, String(key), value);
    skipBlanksAndComments(state);
    if (state.pos < state.len && state.src.charCodeAt(state.pos) === CHAR_COMMA) {
      state.pos++;
      skipBlanksAndComments(state);
    }
  }
  if (state.pos < state.len) state.pos++;
  return result;
};
var parseFlowScalarKey = (state) => {
  const code = state.src.charCodeAt(state.pos);
  if (code === CHAR_SQUOTE) return parseSingleQuoted(state);
  if (code === CHAR_DQUOTE) return parseDoubleQuoted(state);
  if (code === CHAR_LBRACKET) return parseFlowSeq(state);
  if (code === CHAR_LBRACE) return parseFlowMap(state);
  if (code === CHAR_STAR) return parseAlias(state);
  return resolveScalar(parsePlainScalar(state, 0, true));
};
var parseFlowValue = (state) => {
  skipBlanksAndComments(state);
  const code = state.src.charCodeAt(state.pos);
  if (code === CHAR_SQUOTE) return parseSingleQuoted(state);
  if (code === CHAR_DQUOTE) return parseDoubleQuoted(state);
  if (code === CHAR_LBRACKET) return parseFlowSeq(state);
  if (code === CHAR_LBRACE) return parseFlowMap(state);
  if (code === CHAR_STAR) return parseAlias(state);
  const raw = parsePlainScalar(state, 0, true);
  skipSpaces(state);
  if (state.pos < state.len && state.src.charCodeAt(state.pos) === CHAR_COLON) {
    const afterColon = state.pos + 1 >= state.len ? -1 : state.src.charCodeAt(state.pos + 1);
    if (afterColon === -1 || afterColon === CHAR_SPACE || afterColon === CHAR_COMMA || afterColon === CHAR_RBRACKET) {
      const key = String(resolveScalar(raw));
      state.pos++;
      skipBlanksAndComments(state);
      const pair = {};
      const isTerminator = state.pos >= state.len || state.src.charCodeAt(state.pos) === CHAR_COMMA || state.src.charCodeAt(state.pos) === CHAR_RBRACKET || state.src.charCodeAt(state.pos) === CHAR_RBRACE;
      const value = isTerminator ? null : parseFlowValue(state);
      setMapKey(pair, key, value);
      return pair;
    }
  }
  return resolveScalar(raw);
};

// src/internal/block.ts
var MAX_DEPTH = 1e3;
var parseBlockSeq = (state, indent) => {
  const result = [];
  while (state.pos < state.len) {
    const lineIndent = skipNewlinesAndComments(state);
    if (state.pos >= state.len) break;
    if (lineIndent !== indent) {
      state.pos -= lineIndent;
      break;
    }
    const dashNext = state.pos + 1 < state.len ? state.src.charCodeAt(state.pos + 1) : -1;
    if (state.src.charCodeAt(state.pos) !== CHAR_MINUS || dashNext !== -1 && dashNext !== CHAR_SPACE && dashNext !== CHAR_LF && dashNext !== CHAR_CR) {
      state.pos -= lineIndent;
      break;
    }
    state.pos++;
    if (state.pos < state.len && state.src.charCodeAt(state.pos) === CHAR_SPACE)
      state.pos++;
    readAnchorOrTag(state);
    const anchor = state.lastAnchor;
    skipSpaces(state);
    skipComment(state);
    const currentCode = state.pos < state.len ? state.src.charCodeAt(state.pos) : -1;
    let value;
    if (currentCode !== -1 && currentCode !== CHAR_LF && currentCode !== CHAR_CR) {
      value = parseInlineValue(state, indent + 2);
    } else {
      if (currentCode === CHAR_CR) state.pos++;
      if (state.pos < state.len && state.src.charCodeAt(state.pos) === CHAR_LF)
        state.pos++;
      const nextIndent = skipNewlinesAndComments(state);
      value = state.pos < state.len && nextIndent > indent ? parseValue(state, nextIndent, nextIndent) : null;
    }
    result.push(withAnchor(state, anchor, value));
  }
  return result;
};
var parseBlockMap = (state, indent) => {
  const result = {};
  parseBlockMapEntries(state, result, indent, false);
  return result;
};
var parseMapValue = (state, indent) => {
  readAnchorOrTag(state);
  const valAnchor = state.lastAnchor;
  skipSpaces(state);
  skipComment(state);
  const valueCode = state.pos < state.len ? state.src.charCodeAt(state.pos) : -1;
  if (valueCode === -1 || valueCode === CHAR_LF || valueCode === CHAR_CR) {
    if (valueCode === CHAR_CR) state.pos++;
    if (state.pos < state.len && state.src.charCodeAt(state.pos) === CHAR_LF)
      state.pos++;
    const nextIndent = skipNewlinesAndComments(state);
    if (state.pos < state.len && nextIndent > indent)
      return withAnchor(
        state,
        valAnchor,
        parseValue(state, nextIndent, nextIndent)
      );
    // NEW: handle indentless block sequence — "key:\n- item" at the same indent
    if (state.pos < state.len && nextIndent === indent) {
      const dashCode = state.src.charCodeAt(state.pos);
      const dashNext = state.pos + 1 < state.len ? state.src.charCodeAt(state.pos + 1) : -1;
      if (dashCode === CHAR_MINUS &&
          (dashNext === -1 || dashNext === CHAR_SPACE || dashNext === CHAR_LF || dashNext === CHAR_CR)) {
        return withAnchor(state, valAnchor, parseBlockSeq(state, indent));
      }
    }
    return withAnchor(state, valAnchor, null);
  }
  if (valueCode === CHAR_SQUOTE)
    return withAnchor(state, valAnchor, parseSingleQuoted(state));
  if (valueCode === CHAR_DQUOTE)
    return withAnchor(state, valAnchor, parseDoubleQuoted(state));
  if (valueCode === CHAR_LBRACKET)
    return withAnchor(state, valAnchor, parseFlowSeq(state));
  if (valueCode === CHAR_LBRACE)
    return withAnchor(state, valAnchor, parseFlowMap(state));
  if (valueCode === CHAR_PIPE || valueCode === CHAR_GT)
    return withAnchor(state, valAnchor, parseBlockScalar(state, indent));
  if (valueCode === CHAR_STAR)
    return withAnchor(state, valAnchor, parseAlias(state));
  if (valueCode === CHAR_AMP) {
    readAnchorOrTag(state);
    const inlineAnchor = state.lastAnchor;
    skipSpaces(state);
    const value = parseValue(state, indent + 1);
    if (inlineAnchor) state.anchors.set(inlineAnchor, value);
    return withAnchor(state, valAnchor, value);
  }
  return withAnchor(
    state,
    valAnchor,
    resolveScalar(parsePlainScalar(state, indent, false))
  );
};
var parseMapKey = (state) => {
  const keyCode = state.src.charCodeAt(state.pos);
  if (keyCode === CHAR_SQUOTE) return parseSingleQuoted(state);
  if (keyCode === CHAR_DQUOTE) return parseDoubleQuoted(state);
  if (keyCode === CHAR_LBRACKET) return parseFlowSeq(state);
  if (keyCode === CHAR_LBRACE) return parseFlowMap(state);
  if (keyCode === CHAR_STAR) return parseAlias(state);
  const keyStart = state.pos;
  while (state.pos < state.len) {
    const charCode = state.src.charCodeAt(state.pos);
    if (charCode === CHAR_LF || charCode === CHAR_CR) break;
    if (charCode === CHAR_COLON && (state.pos + 1 >= state.len || state.src.charCodeAt(state.pos + 1) === CHAR_SPACE || state.src.charCodeAt(state.pos + 1) === CHAR_TAB || state.src.charCodeAt(state.pos + 1) === CHAR_LF || state.src.charCodeAt(state.pos + 1) === CHAR_CR))
      break;
    if (charCode === CHAR_HASH && state.pos > 0 && (state.src.charCodeAt(state.pos - 1) === CHAR_SPACE || state.src.charCodeAt(state.pos - 1) === CHAR_TAB))
      break;
    state.pos++;
  }
  return resolveScalar(trimEnd(state.src.slice(keyStart, state.pos)));
};
var parseMapEntry = (state, result, indent) => {
  readAnchorOrTag(state);
  const keyAnchor = state.lastAnchor;
  skipSpaces(state);
  const key = parseMapKey(state);
  if (keyAnchor) state.anchors.set(keyAnchor, key);
  skipSpaces(state);
  if (state.pos >= state.len || state.src.charCodeAt(state.pos) !== CHAR_COLON)
    return false;
  state.pos++;
  if (state.pos < state.len && state.src.charCodeAt(state.pos) === CHAR_SPACE)
    state.pos++;
  setMapKey(result, stringifyKey(key), parseMapValue(state, indent));
  return true;
};
var parseExplicitKey = (state, result, indent) => {
  state.pos++;
  if (state.pos < state.len && state.src.charCodeAt(state.pos) === CHAR_SPACE)
    state.pos++;
  readAnchorOrTag(state);
  const keyAnchor = state.lastAnchor;
  skipSpaces(state);
  const keyStartCode = state.pos < state.len ? state.src.charCodeAt(state.pos) : -1;
  const key = keyStartCode === -1 || keyStartCode === CHAR_LF || keyStartCode === CHAR_CR ? null : parseMapKey(state);
  if (keyAnchor) state.anchors.set(keyAnchor, key);
  skipSpaces(state);
  const nextIndent = skipNewlinesAndComments(state);
  const savedPos = state.pos - nextIndent;
  if (nextIndent === indent) {
    const colonCode = state.src.charCodeAt(state.pos);
    const colonNextCode = state.pos + 1 >= state.len ? -1 : state.src.charCodeAt(state.pos + 1);
    if (colonCode === CHAR_COLON && (colonNextCode === -1 || colonNextCode === CHAR_SPACE || colonNextCode === CHAR_LF || colonNextCode === CHAR_CR)) {
      state.pos++;
      if (state.pos < state.len && state.src.charCodeAt(state.pos) === CHAR_SPACE)
        state.pos++;
      setMapKey(result, stringifyKey(key), parseMapValue(state, indent));
      return;
    }
  }
  setMapKey(result, stringifyKey(key), null);
  state.pos = savedPos;
};
var parseBlockMapEntries = (state, result, indent, isInline) => {
  if (isInline) {
    parseMapEntry(state, result, indent);
  }
  while (state.pos < state.len) {
    const lineIndent = skipNewlinesAndComments(state);
    if (state.pos >= state.len) break;
    if (lineIndent !== indent) {
      state.pos -= lineIndent;
      break;
    }
    const lineStartCode = state.src.charCodeAt(state.pos);
    if (lineStartCode === CHAR_QUESTION) {
      const questionNextCode = state.pos + 1 >= state.len ? -1 : state.src.charCodeAt(state.pos + 1);
      if (questionNextCode === -1 || questionNextCode === CHAR_SPACE || questionNextCode === CHAR_LF || questionNextCode === CHAR_CR) {
        parseExplicitKey(state, result, indent);
        continue;
      }
    }
    if (lineStartCode === CHAR_MINUS) {
      const dashNextCode = state.pos + 1 >= state.len ? -1 : state.src.charCodeAt(state.pos + 1);
      if (dashNextCode === -1 || dashNextCode === CHAR_SPACE || dashNextCode === CHAR_LF || dashNextCode === CHAR_CR) {
        state.pos -= lineIndent;
        break;
      }
    }
    if (!parseMapEntry(state, result, indent)) break;
  }
};
var parseInlineValue = (state, indent) => {
  const charCode = state.src.charCodeAt(state.pos);
  if (charCode === CHAR_SQUOTE) return parseSingleQuoted(state);
  if (charCode === CHAR_DQUOTE) return parseDoubleQuoted(state);
  if (charCode === CHAR_LBRACKET) return parseFlowSeq(state);
  if (charCode === CHAR_LBRACE) return parseFlowMap(state);
  if (charCode === CHAR_PIPE || charCode === CHAR_GT)
    return parseBlockScalar(state, indent);
  if (charCode === CHAR_STAR) return parseAlias(state);
  if (charCode === CHAR_MINUS) {
    const dashNextCode = state.pos + 1 >= state.len ? -1 : state.src.charCodeAt(state.pos + 1);
    if (dashNextCode === -1 || dashNextCode === CHAR_SPACE || dashNextCode === CHAR_LF || dashNextCode === CHAR_CR) {
      const result = [];
      while (state.pos < state.len) {
        state.pos++;
        if (state.pos < state.len && state.src.charCodeAt(state.pos) === CHAR_SPACE)
          state.pos++;
        readAnchorOrTag(state);
        const itemAnchor = state.lastAnchor;
        skipSpaces(state);
        skipComment(state);
        const itemCode = state.pos < state.len ? state.src.charCodeAt(state.pos) : -1;
        let value;
        if (itemCode !== -1 && itemCode !== CHAR_LF && itemCode !== CHAR_CR) {
          value = parseInlineValue(state, indent + 2);
        } else {
          if (itemCode === CHAR_CR) state.pos++;
          if (state.pos < state.len && state.src.charCodeAt(state.pos) === CHAR_LF)
            state.pos++;
          const nextIndent2 = skipNewlinesAndComments(state);
          value = state.pos < state.len && nextIndent2 > indent ? parseValue(state, nextIndent2, nextIndent2) : null;
        }
        result.push(withAnchor(state, itemAnchor, value));
        const nextIndent = skipNewlinesAndComments(state);
        if (state.pos >= state.len) break;
        if (nextIndent !== indent) break;
        const savedPos = state.pos - nextIndent;
        if (state.src.charCodeAt(state.pos) !== CHAR_MINUS) {
          state.pos = savedPos;
          break;
        }
        if (state.pos + 1 < state.len) {
          const nextDashCode = state.src.charCodeAt(state.pos + 1);
          if (nextDashCode !== CHAR_SPACE && nextDashCode !== CHAR_LF && nextDashCode !== CHAR_CR) {
            state.pos = savedPos;
            break;
          }
        }
      }
      return result;
    }
  }
  let lookAhead = state.pos;
  let foundColon = false;
  while (lookAhead < state.len) {
    const lookAheadCode = state.src.charCodeAt(lookAhead);
    if (lookAheadCode === CHAR_LF || lookAheadCode === CHAR_CR) break;
    if (lookAheadCode === CHAR_COLON) {
      const afterColonCode = lookAhead + 1 >= state.len ? -1 : state.src.charCodeAt(lookAhead + 1);
      if (afterColonCode === -1 || afterColonCode === CHAR_SPACE || afterColonCode === CHAR_TAB || afterColonCode === CHAR_LF || afterColonCode === CHAR_CR) {
        foundColon = true;
        break;
      }
    }
    if (lookAheadCode === CHAR_HASH && lookAhead > state.pos) {
      const prev = state.src.charCodeAt(lookAhead - 1);
      if (prev === CHAR_SPACE || prev === CHAR_TAB) break;
    }
    lookAhead++;
  }
  if (foundColon) {
    const result = {};
    parseBlockMapEntries(state, result, indent, true);
    return result;
  }
  return resolveScalar(
    parsePlainScalar(state, indent > 0 ? indent - 1 : 0, false)
  );
};
var parseValue = (state, indent, precomputedIndent) => {
  if (++state.depth > MAX_DEPTH)
    throw new RangeError("maximum nesting depth exceeded");
  const lineIndent = precomputedIndent ?? skipNewlinesAndComments(state);
  const lineStart = state.pos - lineIndent;
  const firstCharCode = state.src.charCodeAt(state.pos);
  if ((firstCharCode === CHAR_MINUS || firstCharCode === CHAR_DOT) && state.src.charCodeAt(state.pos + 1) === firstCharCode && state.src.charCodeAt(state.pos + 2) === firstCharCode) {
    const after = state.pos + 3 < state.len ? state.src.charCodeAt(state.pos + 3) : -1;
    if (after === -1 || after === CHAR_SPACE || after === CHAR_LF || after === CHAR_CR || after === CHAR_TAB) {
      state.pos = lineStart;
      state.depth--;
      return null;
    }
  }
  readAnchorOrTag(state);
  const anchor = state.lastAnchor;
  skipSpaces(state);
  const valueCharCode = state.src.charCodeAt(state.pos);
  const effectiveIndent = lineIndent >= indent ? lineIndent : indent;
  const done = (value) => {
    if (anchor) state.anchors.set(anchor, value);
    state.depth--;
    return value;
  };
  if (valueCharCode === CHAR_SQUOTE || valueCharCode === CHAR_DQUOTE) {
    if (isQuotedMapKey(state, valueCharCode)) {
      state.pos = lineStart;
      return done(parseBlockMap(state, effectiveIndent));
    }
    return done(
      valueCharCode === CHAR_SQUOTE ? parseSingleQuoted(state) : parseDoubleQuoted(state)
    );
  }
  if (valueCharCode === CHAR_LBRACKET) return done(parseFlowSeq(state));
  if (valueCharCode === CHAR_LBRACE) return done(parseFlowMap(state));
  if (valueCharCode === CHAR_PIPE || valueCharCode === CHAR_GT)
    return done(parseBlockScalar(state, effectiveIndent));
  if (valueCharCode === CHAR_STAR) return done(parseAlias(state));
  if (valueCharCode === CHAR_MINUS) {
    const dashNextCode = state.pos + 1 >= state.len ? -1 : state.src.charCodeAt(state.pos + 1);
    if (dashNextCode === -1 || dashNextCode === CHAR_SPACE || dashNextCode === CHAR_LF || dashNextCode === CHAR_CR) {
      state.pos = lineStart;
      return done(parseBlockSeq(state, effectiveIndent));
    }
    return done(
      resolveScalar(
        parsePlainScalar(state, indent > 0 ? indent - 1 : -1, false)
      )
    );
  }
  if (valueCharCode === CHAR_QUESTION) {
    const questionNextCode = state.pos + 1 >= state.len ? -1 : state.src.charCodeAt(state.pos + 1);
    if (questionNextCode === -1 || questionNextCode === CHAR_SPACE || questionNextCode === CHAR_LF || questionNextCode === CHAR_CR) {
      state.pos = lineStart;
      return done(parseBlockMap(state, effectiveIndent));
    }
    return done(
      resolveScalar(
        parsePlainScalar(state, indent > 0 ? indent - 1 : -1, false)
      )
    );
  }
  if (hasColonInLine(state)) {
    state.pos = lineStart;
    return done(parseBlockMap(state, effectiveIndent));
  }
  return done(
    resolveScalar(parsePlainScalar(state, indent > 0 ? indent - 1 : -1, false))
  );
};
var isQuotedMapKey = (state, quoteCharCode) => {
  let lookAhead = state.pos + 1;
  while (lookAhead < state.len && state.src.charCodeAt(lookAhead) !== quoteCharCode) {
    if (quoteCharCode === CHAR_DQUOTE && state.src.charCodeAt(lookAhead) === CHAR_BACKSLASH)
      lookAhead++;
    lookAhead++;
  }
  if (lookAhead < state.len) lookAhead++;
  while (lookAhead < state.len) {
    const code = state.src.charCodeAt(lookAhead);
    if (code !== CHAR_SPACE && code !== CHAR_TAB) break;
    lookAhead++;
  }
  if (lookAhead >= state.len || state.src.charCodeAt(lookAhead) !== CHAR_COLON)
    return false;
  const afterColon = lookAhead + 1 >= state.len ? -1 : state.src.charCodeAt(lookAhead + 1);
  return afterColon === -1 || afterColon === CHAR_SPACE || afterColon === CHAR_LF || afterColon === CHAR_CR || afterColon === CHAR_TAB;
};
var hasColonInLine = (state) => {
  const savedPos = state.pos;
  let lookAhead = state.pos;
  const quoteCode = state.src.charCodeAt(lookAhead);
  if (quoteCode === CHAR_DQUOTE || quoteCode === CHAR_SQUOTE) {
    lookAhead++;
    while (lookAhead < state.len && state.src.charCodeAt(lookAhead) !== quoteCode) {
      if (state.src.charCodeAt(lookAhead) === CHAR_BACKSLASH && quoteCode === CHAR_DQUOTE)
        lookAhead++;
      lookAhead++;
    }
    if (lookAhead < state.len) lookAhead++;
    while (lookAhead < state.len) {
      const code = state.src.charCodeAt(lookAhead);
      if (code !== CHAR_SPACE && code !== CHAR_TAB) break;
      lookAhead++;
    }
    if (lookAhead < state.len && state.src.charCodeAt(lookAhead) === CHAR_COLON) {
      const afterColonCode = lookAhead + 1 >= state.len ? -1 : state.src.charCodeAt(lookAhead + 1);
      return afterColonCode === -1 || afterColonCode === CHAR_SPACE || afterColonCode === CHAR_LF || afterColonCode === CHAR_CR || afterColonCode === CHAR_TAB;
    }
    state.pos = savedPos;
    return false;
  }
  while (lookAhead < state.len) {
    const code = state.src.charCodeAt(lookAhead);
    if (code === CHAR_LF || code === CHAR_CR) break;
    if (code === CHAR_COLON) {
      const afterColonCode = lookAhead + 1 >= state.len ? -1 : state.src.charCodeAt(lookAhead + 1);
      if (afterColonCode === -1 || afterColonCode === CHAR_SPACE || afterColonCode === CHAR_TAB || afterColonCode === CHAR_LF || afterColonCode === CHAR_CR)
        return true;
    }
    if (code === CHAR_HASH && lookAhead > 0) {
      const prev = state.src.charCodeAt(lookAhead - 1);
      if (prev === CHAR_SPACE || prev === CHAR_TAB) break;
    }
    lookAhead++;
  }
  return false;
};

// src/index.ts
var parse = (input) => {
  if (typeof input !== "string") throw new TypeError("source is not a string");
  const src = input.charCodeAt(0) === CHAR_BOM ? input.slice(1) : input;
  if (src.includes("\0")) throw new SyntaxError("null byte is not allowed");
  if (NON_PRINTABLE.test(src))
    throw new SyntaxError("non-printable characters are not allowed");
  const state = {
    src,
    len: src.length,
    pos: 0,
    depth: 0,
    lastAnchor: void 0,
    anchors: /* @__PURE__ */ new Map()
  };
  skipDirectivesAndMarkers(state);
  if (state.pos >= state.len) return null;
  return parseValue(state, 0);
};
